# =============================================================================
# Structo URL Shortener — Environment Variables
# Copy this file to .env and fill in your values
# =============================================================================

# Django
DJANGO_SETTINGS_MODULE=config.settings.development
SECRET_KEY=change-me-to-a-random-secret-key

# Database (for production / docker-compose)
DATABASE_URL=postgres://structo:structo@localhost:5432/structo

# Site
SITE_DOMAIN=http://localhost:8000
ALLOWED_HOSTS=*

# URL Shortener
SHORTENER_CODE_LENGTH=7
SHORTENER_MAX_RETRIES=5

# CORS (comma-separated origins for production)
CORS_ALLOWED_ORIGINS=

# CSRF (comma-separated origins for production)
CSRF_TRUSTED_ORIGINS=

# Logging
LOG_LEVEL=DEBUG
